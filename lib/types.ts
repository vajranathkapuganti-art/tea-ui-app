export type Screen = 'home' | 'menu' | 'product' | 'account';

export type HeroItem = {
  name: string;
  subtitle: string;
};

export type FeaturedDrink = {
  name: string;
  shortLabel: string;
  price: string;
};
